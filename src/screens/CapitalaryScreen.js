import { View } from 'react-native';
import styles from '../libs/styles';
import Background from '../components/Background';

export default function CapitalaryScreen({}) {
  return (
    <Background>
      <View style={styles.container}>
      </View>
    </Background>
  );
}
